{
  "name": "json-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --port $PORT --middlewares ./empty.js"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
